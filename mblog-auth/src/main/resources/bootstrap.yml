server:
  port: 12002
spring:
  application:
    name: mblog-auth
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        group: mblog
      config:
        server-addr: localhost:8848
        file-extension: yml
        group: mblog
